.build/
.swiftpm/
dist/
.DS_Store
*.xcodeproj
config.json
.env
*.key

# local AI-agent / index state (never publish)
# .codegraph is a symlink into ~/.omo, so match without a trailing slash
.codex/
.codegraph

# local marketing copy (thread/social posts) — not published
thread-posts.md
